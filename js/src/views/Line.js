define([
    'src/core/View',
    // 'src/views/BarView',
], function(BaseView) {
    var App = BaseView.extend({
        initialize: function(option) {
            
        },
        draw: function() {
            // HF.stage.update();
        }
    });
    return App;
});